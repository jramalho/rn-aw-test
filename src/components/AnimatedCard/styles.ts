import { StyleSheet } from 'react-native';

// AnimatedCard uses Card's styles, so this file is minimal
export const styles = StyleSheet.create({
  // No additional styles needed - animation is handled by Reanimated
});
